<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SimpleCalculator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SimpleCalculator" parent="Base.Theme.SimpleCalculator" />

    <style name="operatorbutton" parent="commonbutton">
        <item name="background">@color/white</item>
        <item name="android:textColor">@color/btnYellow</item>
    </style>
    <style name="digitbutton" parent="commonbutton">
        <item name="background">@color/btnWhite</item>
        <item name="android:textColor">@color/black</item>
    </style>
    <style name="commonbutton" parent="Widget.MaterialComponents.ExtendedFloatingActionButton" >
        <item name="android:layout_width">70dp</item>
        <item name="android:layout_height">70dp</item>
        <item name="backgroundTint">@color/btnWhite</item>
        <item name="cornerRadius">20dp</item>
        <item name="android:textSize">20dp</item>
        <item name="android:layout_margin">10dp</item>
    </style>

</resources>